<p style="color: green"><%= notice %></p>

<h1>Guitar bodies</h1>

<div id="bodies">
  <% @bodies.each do |body| %>
    <%= render body %>
    <p>
      <%= link_to "Show this body", body %>
    </p>
  <% end %>
</div>

<%= link_to "Add new body", new_dict_body_path %>
